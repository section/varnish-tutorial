apiVersion: v1
kind: Service
metadata:
  labels:
    app: spa-service
  name: spa-service
  namespace: default
spec:
  ports:
  - name: 80-3000
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: spa
  sessionAffinity: None
  type: ClusterIP